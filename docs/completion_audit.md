# Completion Audit

Checked on 6 June 2026 against the original instruction and the current local release candidate.

## Proven Complete

- Idea selected and documented with expanded product discovery, competitor notes, risk analysis and RTF-aligned candidate evaluation: `docs/product_decision.md`.
- MVP spec documented and expanded against the original RTF structure: `docs/product_spec.md`.
- Android stack documented: `docs/tech_stack_decision.md`.
- `AGENTS.md` expanded with setup, commands, architecture, code/UI/asset/documentation rules, release verifier responsibilities and done criteria.
- Native Android project implemented: Kotlin + Compose + Material 3.
- Neutral package id: `com.qgrid.mobile`.
- Core loop works: deterministic levels, independently solver-verified reachability, selection, sum target 56, exceeded feedback with input lock until undo/reset, undo, reset, current-selection-aware hint with repair feedback, win state and immutable post-win reducer behavior.
- Reducer input hardening rejects coordinates outside the current board without mutating selection or calling `Board.cellAt()` on invalid input.
- Game model validation rejects inconsistent board cell positions, non-positive cell values, board sizes outside the readable 4x4-6x6 release range, level ids outside the released set, mismatched level/board sizes, empty/repeated/out-of-board/non-adjacent solution paths, canonical paths that do not sum to 56 and inconsistent `GameState` status/selection/playable hint metadata, so hints and `Board.cellAt()` cannot silently operate on inconsistent game data.
- Result panel always offers an explicit post-win action: `Следующий уровень` is shown only when a higher available level id exists, otherwise the player gets `К уровням`; the next action resolves from the won `gameState.level` rather than a stale `currentLevel`.
- Russian UI strings: `app/src/main/res/values/strings.xml`; post-refactor search found no Russian UI string literals in `app/src/main/java`, and `tools/verify_release.py` now gates Android string resources for non-empty Russian copy without placeholder/debug/test markers.
- Local progress/settings: DataStore Preferences; invalid completed level ids and corrupted completed-level CSV strings are ignored, `lastLevelId` is clamped to the valid 1-36 range, direct level navigation, home `Продолжить`, progress totals, level-picker order and final-level result state resolve against distinct sorted available level ids, and DataStore read `IOException` falls back to safe default preferences. The home primary action now resumes the last selected incomplete level, falls back to the first incomplete level when the last selected level is complete, and keeps completed-all replay on the last level.
- No `INTERNET`, no `ACCESS_NETWORK_STATE` and no dangerous/platform runtime permissions: verifier checks source manifest, merged debug/release manifests and debug APK permissions; APK only contains the AndroidX-generated app-private dynamic receiver permission.
- Android backup is disabled: source and merged release manifests contain `android:allowBackup="false"`, and `aapt dump xmltree` on the rebuilt debug APK shows `allowBackup=0x0` with no backup/data-extraction rule attributes.
- Manifest entry-point hygiene is verifier-gated: source manifest declares exactly one launcher activity (`.MainActivity`, `exported=true`, MAIN/LAUNCHER) and no services, receivers or providers.
- No backend, ads, analytics, payments, accounts or UGC.
- Unit tests pass: `./gradlew test`.
- Debug build passes: `./gradlew assembleDebug`.
- Lint passes with no issues: `./gradlew lint`.
- Signed release AAB builds: `./gradlew bundleRelease`.
- Production AAB cleanliness is checked by `tools/verify_release.py`: no project `.debug` package id, androidTest, JUnit, Espresso or Compose UI-test markers are present in the release artifact.
- Artifact and Play asset size budgets are checked by `tools/verify_release.py`; current release AAB is 2,930,928 bytes and current debug APK is 19,833,279 bytes.
- Debug APK and release AAB freshness are checked by `tools/verify_release.py`; stale artifacts after app source, Gradle, ProGuard or signing-input changes fail the release gate.
- Emulator smoke tests pass: `./gradlew connectedDebugAndroidTest`; latest connected run finished 10 tests covering product name, accessible game board/controls, hint repair feedback, settings options, About/privacy policy text, level completion, three-level progression, highest-level result fallback, result replay, game-back routing, progress persistence and Activity recreation.
- Release APK installed on API 35 emulator: `./gradlew installRelease`.
- Latest release relaunch smoke check on API 36: `tools/capture_release_relaunch_smoke.py` installed release package `com.qgrid.mobile`, cleared app data, completed onboarding, verified home `Прогресс` after HOME/background return, verified home `Прогресс` and visible home start action after force-stop/relaunch, confirmed onboarding was not visible after relaunch and found no matching app crash-buffer entries. Evidence: `docs/qa_artifacts/release_relaunch_state.txt` and `docs/qa_artifacts/release_relaunch_home.png`.
- Offline release smoke is locally proven on API 35: `Medium_Phone_API_35_Default` / `emulator-5554` ran release package `com.qgrid.mobile` with `airplane-mode enabled`, `airplane_mode_on=1` and `Wi-Fi is disabled`; after a 202 ms cold launch, UIAutomator reached onboarding, home `Прогресс` and game-board `Цель`/`Клетка`, with no matching app crash entries and visual evidence in `docs/qa_artifacts/offline_release_smoke_onboarding.png` plus `docs/qa_artifacts/offline_release_smoke_game.png`.
- Phone screenshots captured from real release app: `play_store/screenshots/phone`.
- Large/tablet screenshots captured from real release app: `play_store/screenshots/tablet`.
- Google Play store icon created and verified with built-in ImageGen: `play_store/icon/play_icon_512.png`, 512x512 RGBA, under 1024KB, full-square with no transparent pixels, no text or characters.
- Feature graphic concept was visually audited, archived to `play_store/archive/feature_graphic_concept.png`, and rejected for release upload.
- Replacement feature graphic created and verified: `play_store/feature_graphic.png`, 1024x500 RGB/24-bit PNG without alpha, using a no-text ImageGen background plus a real release gameplay crop.
- Phone and large/tablet screenshots converted and verified as RGB/24-bit PNGs without alpha.
- Small-screen/font-scale exploratory pass completed on 720x1280, density 320, `font_scale=1.3`; onboarding, home and About/privacy were visually checked, including top/bottom About screenshots after the in-app privacy UI refactor.
- Large/wide exploratory pass completed on 2400x1080 landscape and 1600x2560 tall overrides after responsive onboarding fix.
- Game landscape/font-scale pass completed on 2400x1080 with `font_scale=1.3`; game controls and all 25 cells are visible in the same viewport.
- Relaunch after completing a level verified on debug build: after the persistence instrumentation flow, `am force-stop` + `am start` returned to home with `Прогресс`, `Продолжить` and `1 / 36`.
- Completion of several levels verified: connected instrumentation completed levels 1, 2 and 3 sequentially through the UI and confirmed accumulated progress.
- Highest-level result fallback verified: connected instrumentation completed level 36 through the real UI, confirmed `К уровням`, confirmed `Следующий уровень` was absent and returned to the levels screen.
- Package/signing identifiers neutralized: `com.qgrid.mobile`, signing `QGRID_UP`/`QuietGrid Upload`.
- Cell accessibility descriptions include selected, hint and exceeded states; connected instrumentation verifies the hint state is exposed.
- Non-TalkBack accessibility-tree audit completed on API 35 at `font_scale=1.3`; onboarding, home, settings and game exposed expected Russian labels/descriptions, and the game exposed all 25 cells.
- TalkBack exploratory pass completed on `Medium_Phone_API_36` Play Store AVD with Android Accessibility Suite installed and `TalkBackService` enabled. The release app was driven through onboarding, home, game, settings and About/privacy with TalkBack focus tap + double-tap activation; UI trees exposed Russian labels, all 25 game cell descriptions and privacy text while `touchExplorationEnabled=true`.
- Google Play listing/data-safety/privacy-policy drafts created: `play_store`.
- Google Play upload manifest, upload checksums, Play Console field handoff, App content answer sheet, owner-input checklist, privacy hosting checklist and ready-to-host privacy policy HTML created: `play_store/upload_manifest.md`, `play_store/upload_checksums.md`, `play_store/play_console_submission_ru.md`, `play_store/app_content_answers_ru.md`, `play_store/owner_release_inputs.md`, `play_store/privacy_policy_hosting_checklist.md`, `play_store/privacy_policy_ru.html`; HTML parser check passed.
- Store metadata is verifier-checked for app name, short description, full description and release notes length budgets, placeholder markers and exact sync between `play_store/listing_ru.md` and `play_store/play_console_submission_ru.md`.
- Privacy/data-safety/content-rating handoff is verifier-checked for no data collection/sharing, no analytics, no crash reporting, no ads/payments/accounts/device identifiers/UGC, no `INTERNET`/`ACCESS_NETWORK_STATE`, Android backup disabled and neutral content-rating posture.
- In-app About/privacy copy is verifier-checked for no collection/sharing, local-only progress/settings, Android backup disabled and local deletion instructions.
- App content answer sheet is verifier-checked for no restricted access, no ads, no collected/shared user data, no gambling/UGC/online interaction, non-child-directed target-audience recommendation, no financial/health/government features and no in-app generative AI.
- Public signing certificate handoff created: `play_store/signing_certificate_report.md`; it documents upload certificate fingerprints without signing secrets.
- Secret hygiene is verifier-checked: release-facing text files are scanned for common API key/token prefixes, private-key blocks, hardcoded bearer tokens, suspicious secret assignments and local/private dev URLs.
- Google Play preview asset alt text created and length-checked: `play_store/asset_alt_text_ru.md`.
- Google Play official source check documented: `docs/google_play_sources.md`.
- Google Play checklist expanded with build/signing, verification commands, asset checklist, privacy/data-safety, permissions, content rating, target audience, testing instructions, known limitations and manual Play Console actions.
- Requirements traceability matrix created and verifier-gated: `docs/requirements_traceability.md` maps all 16 original RTF phases and final completion criteria to local evidence and external manual gates.
- Upload checksum manifest created and verifier-gated: `play_store/upload_checksums.md` records byte counts and SHA-256 fingerprints for the signed AAB, store icon, feature graphic and release phone/tablet screenshots.
- Release verifier created and passing: `tools/verify_release.py`; it now resolves local Android tools portably and checks required docs, `.gitignore`, private signing-file permissions, signing report, merged manifests, binary APK permissions, release AAB debug/test cleanliness and size budgets.
- Final clean verification passes: `./gradlew clean assembleDebug assembleDebugAndroidTest`, then after the latest localization/resource refactor, final feature graphic replacement, backup hardening, Play handoff docs and release verifier addition `./gradlew test lint bundleRelease`, `./gradlew assembleDebug assembleDebugAndroidTest`, `./tools/verify_release.py`, and a retry of `./gradlew connectedDebugAndroidTest` with 5 passing tests.
- Latest incremental verification after Play Console handoff and verifier portability update: `./gradlew test lint bundleRelease`, `./tools/verify_release.py` and `./gradlew connectedDebugAndroidTest` passed; connected run finished 5 tests on API 35 and the emulator was stopped afterward.
- Latest incremental verification after exceeded-state hardening: `./gradlew test`, `./gradlew lint bundleRelease`, `./tools/verify_release.py` and `./gradlew connectedDebugAndroidTest` passed; connected run finished 5 tests on API 35 and the emulator was stopped afterward.
- Latest incremental verification after solver reachability hardening: `./gradlew test`, `./gradlew lint bundleRelease` and `./tools/verify_release.py` passed.
- Latest incremental verification after store metadata verifier hardening: `./tools/verify_release.py` and `./gradlew test lint bundleRelease` passed.
- Latest incremental verification after privacy/data-safety verifier hardening: `./tools/verify_release.py` and `./gradlew test lint bundleRelease` passed.
- Latest incremental verification after Android resource localization hardening: `./tools/verify_release.py` and `./gradlew test lint bundleRelease` passed.
- Latest incremental verification after secret hygiene verifier hardening: `./tools/verify_release.py` and `./gradlew test lint bundleRelease` passed.
- Latest incremental verification after requirements traceability hardening: `./tools/verify_release.py` and `./gradlew test lint bundleRelease` passed.
- Latest incremental verification after upload checksum hardening: `./gradlew test lint bundleRelease` passed, then `./tools/verify_release.py` passed and confirmed the final AAB/assets match `play_store/upload_checksums.md`.
- Latest incremental verification after progress normalization hardening: `./gradlew test lint bundleRelease` passed, then `./tools/verify_release.py` passed with the updated AAB checksum.
- Latest incremental verification after post-win reducer hardening: `./gradlew test lint bundleRelease` passed, then `./tools/verify_release.py` passed with the updated AAB checksum.
- Latest incremental verification after level navigation hardening: `LevelNavigation` resolves direct and next-level navigation against distinct available level ids rather than list size, including sparse/reordered level lists, duplicate level ids and null current-level state. `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed with the then-current 9-test suite; the current connected gate is now the 10/10 API 36 suite.
- Latest incremental verification after DataStore read fallback and neutral package rename hardening: `./gradlew test lint bundleRelease`, `./gradlew assembleDebug`, `./tools/verify_release.py`, `./gradlew connectedDebugAndroidTest` and `./gradlew installRelease` passed. Production package is `com.qgrid.mobile`; debug package is `com.qgrid.mobile.debug`; the release verifier rejects the previous package/path markers in release-facing files and the production AAB. The post-rename connected run finished 5 tests on `Medium_Phone_API_35_Default(AVD) - 15` with 0 failures, 0 errors and 0 skipped tests; release smoke QA cold-launched `com.qgrid.mobile/.MainActivity`, confirmed it was foreground/top resumed, found `Линия 56` and `Начать` in the UI tree and found no matching app crash entries.
- Latest incremental verification after Play Console app-content answer hardening: official Play Help sources for Data Safety, User Data/privacy policy, Content Ratings and Target audience were rechecked, `play_store/app_content_answers_ru.md` was added, and `tools/verify_release.py` now requires and validates it.
- Latest incremental verification after in-app privacy hardening: About/privacy Android strings mirror the Play privacy posture, instrumentation coverage opens About/privacy and checks the key statements, and `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py`, `./gradlew connectedDebugAndroidTest` and `./gradlew installRelease` passed. The signed AAB checksum was refreshed in `play_store/upload_checksums.md`; connected run finished 6 tests with 0 failures, 0 errors and 0 skipped tests, and release smoke cold-launched `com.qgrid.mobile/.MainActivity` in 356 ms with no matching app crash entries.
- Latest compact About/privacy pass after in-app privacy UI refactor: release APK installed on a cold-boot API 35 AVD at 720x1280, density 320, `font_scale=1.3`; onboarding and home were reachable, `О проекте` was visible/tappable, About/privacy top and scrolled-bottom screenshots were captured, and the bottom capture confirmed the display override remained 720x1280 before and after capture.
- Latest in-app privacy UI refactor: About/privacy privacy text was split from one long paragraph into short scannable points for local data, no services, Android backup disabled and deletion. `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py`, `./gradlew connectedDebugAndroidTest` and `./gradlew installRelease` passed; connected run finished 6 tests with 0 failures, 0 errors and 0 skipped tests, and release smoke cold-launched `com.qgrid.mobile/.MainActivity` in 356 ms with no matching app crash entries.
- Latest compact About/privacy artifact hardening: `tools/verify_release.py` now checks the two 720x1280 QA screenshots and QA documentation markers; follow-up `./tools/verify_release.py` and `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease` passed.
- Latest large/tablet screenshot hardening: `play_store/screenshots/tablet` contains five real release-app captures exported as Play-ready RGB/24-bit PNGs; `play_store/upload_manifest.md`, `play_store/upload_checksums.md`, `play_store/asset_alt_text_ru.md`, `play_store/screenshots/manifest.md`, `docs/asset_manifest.md`, `docs/google_play_checklist.md` and `tools/verify_release.py` were updated. Follow-up `./tools/verify_release.py` and `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease` passed.
- Latest TalkBack hardening: API 36 Play Store AVD TalkBack pass added five QA artifacts under `docs/qa_artifacts`; docs and `tools/verify_release.py` now gate the TalkBack evidence. Audio/speech output quality was not judged by ear because the local pass used a headless AVD.
- Latest Google Play source refresh: official Google Play / Android Developers sources for target API, AAB upload format, personal-account closed testing, preview assets, Data Safety, User Data/privacy policy, content rating and target audience were rechecked on 4 June 2026. `docs/google_play_sources.md` now records the current requirements and `tools/verify_release.py` gates the source-audit markers.
- Latest Google Play source spot-check on 6 June 2026: the same official Play/Android source set was rechecked after the final ImageGen icon replacement, including the full-square Google Play icon design requirement, signed AAB upload format, Data Safety/no-data declaration, privacy-policy URL/text requirement, content rating, target audience and personal-account closed-testing gates.
- Latest Google Play source spot-check on 11 June 2026: official Play/Android source pages were rechecked for target API 35+ submission posture, AAB upload, preview assets, Play icon requirements, Data Safety, User Data/privacy policy, content rating, target audience, personal-account closed testing and the latest policy announcements. No local product change was required because the game already targets SDK 36, ships a signed AAB, keeps no-data/no-network/no-ads posture and has no Contacts, Location, Health, prediction-market or News app scope.
- Latest final local gate after 11 June source audit: `./tools/run_final_local_gate.py --include-hosted-privacy` passed with `final_local_gate_ok`, including `release_verification_ok`, `play_upload_archive_existing_ok`, `publication_readiness_local_ready_external_pending`, hosted privacy URL validation, `privacy_policy_local_ok`, `signing_backup_input_ok` and `signing_backup_evidence_packet_ok`.
- Latest privacy/signing handoff date refresh: `play_store/privacy_policy_ru.md`, `play_store/privacy_policy_ru.html` and `play_store/signing_certificate_report.md` now use the 6 June 2026 current-candidate check date, and `tools/verify_release.py` gates those current markers.
- Latest completion/traceability date refresh: this completion audit and `docs/requirements_traceability.md` now carry the 6 June 2026 current-candidate audit date, and `tools/verify_release.py` gates both current markers.
- Latest verification after source refresh: `./tools/verify_release.py` passed with `check_google_play_sources`; `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease` passed; an already-running external `project_betano_emulator` dropped offline before instrumentation, then `Medium_Phone_API_35_Default` was cold-started and `./gradlew connectedDebugAndroidTest` passed 6/6 tests on API 35.
- Latest product/performance evidence hardening: competitor Google Play listings in `docs/product_decision.md` were rechecked on 4 June 2026; `docs/performance_notes.md`, `play_store/upload_checksums.md` and `play_store/signing_certificate_report.md` were refreshed to the current local candidate. `tools/verify_release.py` now gates both product-decision source markers and performance-note size markers. Follow-up `./tools/verify_release.py` and `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease` passed.
- Latest README handoff hardening: `README.md` now reflects the 4 June 2026 local release-candidate checks, current neutral package, TalkBack artifacts and remaining Play/manual gates. `tools/verify_release.py` now gates these README handoff markers. Follow-up `./tools/verify_release.py` and `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease` passed.
- Latest asset-handoff hardening: `tools/verify_release.py` now requires `docs/rejected_assets.md` and `play_store/screenshots/manifest.md`, verifies release-ready/source-only/rejected/QA asset markers, and checks that source/rejected/debug/test/signing/local files stay out of the Play upload section while remaining in the do-not-upload section. Follow-up `./tools/verify_release.py` passed.
- Current API 36 connected hardening: `Medium_Phone_API_36` headless Play Store AVD booted as Android 16 / API 36; `./gradlew connectedDebugAndroidTest` passed 7/7 tests on `Medium_Phone_API_36(AVD) - 16` with 0 skipped and 0 failed tests, then the AVD was stopped with `adb emu kill`. This strengthens target/compile SDK 36 readiness beyond the earlier API 35 connected run and now includes the final-level result fallback.
- Latest privacy-policy handoff hardening: `play_store/privacy_policy_ru.md` and `play_store/privacy_policy_ru.html` were refreshed to 4 June 2026, and `tools/verify_release.py` now checks the source markdown, HTML and hosting checklist for no-data/no-sharing/no-ads/no-analytics/no-payments posture, local-only progress/settings, Android backup disabled, deletion instructions, privacy inquiry mechanism and public HTTPS hosting requirements. Follow-up `./tools/verify_release.py` passed.
- Latest Play Console submission hardening: `play_store/play_console_submission_ru.md` now includes dedicated large/tablet screenshot upload paths in addition to phone screenshots. `tools/verify_release.py` added `check_play_console_submission_handoff` for package identity, listing handoff, upload artifacts, phone/tablet screenshots, privacy/data-safety posture, AI-content note and manual Play gates. Follow-up `./tools/verify_release.py` passed.
- Latest board model hardening: `Board` now validates cell-index position consistency and positive cell values; unit tests cover both rejection paths. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease` passed and upload checksum/performance evidence was refreshed for the rebuilt AAB.
- Latest level model hardening: `LevelDefinition` now validates released level ids, board-size consistency, non-empty in-board unique adjacent solution paths and canonical path sum 56. Unit tests cover invalid ids, mismatched size, empty path, repeated solution cells, out-of-board solution cells, non-adjacent solution cells and non-target sums; `tools/verify_release.py` now gates the source/test markers. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5560 ./gradlew connectedDebugAndroidTest` on API 36 passed.
- Latest reducer input hardening: `Board.contains()` and `GameMessage.NOT_ON_BOARD` let `GameReducer.selectCell()` reject out-of-board coordinates at start or after a selection without crash or selection mutation. Unit tests cover both paths, and `tools/verify_release.py` gates the reducer/test markers.
- Latest instrumentation order-independent hardening: a cold API 36 connected run exposed that `settingsScreenExposesAccessibleOptions` could inherit a non-home screen from a previous test; `Line56AppSmokeTest.ensureHome()` now returns through `Назад` before asserting home. Follow-up `ANDROID_SERIAL=emulator-5560 ./gradlew connectedDebugAndroidTest` passed 6/6 tests on Android 16/API 36 with 0 skipped and 0 failed tests.
- Latest progress codec hardening: completed-level CSV parsing/serialization now lives in pure `ProgressRules`, trims whitespace, ignores non-numeric/out-of-range ids and serializes sanitized ids in stable order before DataStore writes. Unit tests cover corrupted CSV and stable serialization, and `tools/verify_release.py` gates repository usage of the shared codec. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5562 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed.
- Latest home-state navigation hardening: `AppUiState` now computes progress count and `nextLevelId` from distinct sorted available level ids, matching `LevelNavigation` behavior for sparse/reordered level lists. Unit tests cover progress count and `Продолжить` selection, and `tools/verify_release.py` gates the source/test markers. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5562 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed with 6/6 connected tests, 0 skipped and 0 failed.
- Latest level-status copy hygiene: unlocked level tiles now use the `available` Android string resource for `Доступен`; the misleading old `locked` resource name was removed, and `tools/verify_release.py` gates this invariant. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed with 6/6 connected tests, 0 skipped and 0 failed; the prior API 36 attempt failed before tests because the emulator process crashed after boot.
- Latest level-list UI hardening: `AppUiState` now exposes `displayLevels`, `totalLevelCount`, `currentGameIsLastLevel` and `currentGameCompletesAllLevels`, so home progress totals, the level picker, next-level button visibility and all-complete result copy no longer depend on raw `levels.size` or raw list order. Unit tests cover sparse/reordered/duplicate level lists and the highest-level-with-incomplete-earlier-levels case, and `tools/verify_release.py` gates the source/UI markers. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed with 6/6 connected tests, 0 skipped and 0 failed.
- Latest continue-routing hardening: `lastLevelId` is no longer only stored for privacy/progress metadata. `AppUiState.nextLevelId` uses the last selected incomplete level first, then the first incomplete level, then the last level for completed-all replay. `shouldShowContinueAction` keeps the home primary label aligned with that route. Unit tests and `tools/verify_release.py` gate the source/test/UI markers. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed with 6/6 connected tests, 0 skipped and 0 failed.
- Latest all-complete result hardening: result panel text now separates `hasNextLevel` from `allLevelsComplete`, so passing the highest-numbered level alone does not claim every level is complete. The all-complete copy is shown only when the saved progress plus the just-won current level covers all available level ids. Unit tests and `tools/verify_release.py` gate this invariant. Follow-up `./gradlew test`, `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed with 6/6 connected tests, 0 skipped and 0 failed.
- Latest result-action fallback connected hardening: result panel action now uses `AppUiState.currentGameHasNumberedNextLevel`, and the connected suite proves the final-level UI path by completing level 36, seeing `К уровням`, not seeing `Следующий уровень` and returning to the levels screen. `tools/verify_release.py` gates the connected-test marker and validates the generated connected XML report. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` on Android 16/API 36 passed with 7/7 tests, 0 skipped and 0 failed.
- Latest signing report hardening: `tools/verify_release.py` now compares `play_store/signing_certificate_report.md` to the actual signed release AAB certificate using `keytool -printcert -jarfile`, including owner, serial number, SHA-256 fingerprint and signature algorithm.
- Latest dependency-surface hardening: `tools/verify_release.py` now gates `app/build.gradle.kts` against ads, analytics, crash SDKs, billing, auth/location Play Services, backend/network clients and image-loading SDK markers while requiring the expected lightweight Compose/DataStore/test stack.
- Latest signing-input hardening: `tools/verify_release.py` now checks local `local.properties`, `keystore.properties` and `private/signing/*.p12` permissions, and checks `keystore.properties`, when present, for `private/signing/qgrid-upload.p12`, `keyAlias=qgrid_upload` and absence of the old obvious signing path.
- Latest reduced-motion polish: selected, hinted and exceeded cells now have short color/elevation transitions, with zero-duration updates when `Меньше движения` is enabled. `tools/verify_release.py` gates this source invariant. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` passed with 7/7 connected tests on Android 16/API 36, 0 skipped and 0 failed.
- Latest discreet package identity update: production/debug package ids and Kotlin namespace now use `com.qgrid.mobile` / `com.qgrid.mobile.debug`; source directories moved to `app/src/*/java/com/qgrid/mobile`, release docs were refreshed, and `tools/verify_release.py` rejects the old package/path markers in source/docs and the release AAB. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py`, `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` and `ANDROID_SERIAL=emulator-5564 ./gradlew installRelease`/release cold launch passed; connected finished 7/7 tests on API 36 and final `adb devices` was empty after stopping `emulator-5564`.
- Latest haptics polish: cell feedback now runs from accepted selected-path growth rather than the tap callback, and victory feedback now runs only on transition into `WON`. `tools/verify_release.py` gates these source markers so invalid/rejected taps and restored won screens do not create misleading haptic feedback. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` passed; connected finished 7/7 tests on API 36 and final `adb devices` was empty after stopping `emulator-5564`.
- Latest optimistic completion progress hardening: `GameViewModel.selectCell()` now updates in-memory progress immediately when a level transitions into `WON`, using `ProgressRules.withCompletedLevel()` so Home/progress state reflects the win before the asynchronous DataStore emission returns. New valid completions also record the completed level as `lastLevelId`, while invalid optimistic ids are ignored and already-completed replay remains idempotent; `ProgressRepository.markLevelCompleted()` writes the completed id and last-level id in the same DataStore edit. Unit coverage verifies the helper preserves settings, sanitizes progress and records a new completed level as last played; `tools/verify_release.py` gates the source/test markers. Follow-up `./gradlew test lint assembleDebug bundleRelease`, `ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest` on API 36 and `./tools/verify_release.py` passed; the signed AAB checksum was refreshed in `play_store/upload_checksums.md`.
- Latest progress integrity regression guard: `ProgressRules.withCompletedLevel()` now has explicit unit coverage for ignoring an invalid optimistic completion id and for treating replayed already-completed levels as idempotent without mutating existing completed progress, `lastLevelId` or settings. `tools/verify_release.py` gates these test markers.
- Latest game-back route hardening: `GameViewModel` now remembers whether a game was opened from home or level selection. Header/system back returns to that entry screen, while result action `К уровням` always opens the level list. Connected coverage `gameBackReturnsToEntryScreen` proves both paths through the real UI, and `tools/verify_release.py` gates the source/test markers plus the connected report.
- Latest hint logic hardening: `LevelSolver.findPathFromSelection()` powers hints from the current selected line, not just a stored canonical path. Unit tests cover a non-canonical playable line, a dead-end line with `HINT_REPAIR`, and solver continuation/null behavior; connected coverage now proves the repair message and absence of stale hint highlighting on a real level-1 dead-end line. `tools/verify_release.py` gates the source and test markers. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `./tools/verify_release.py` and `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` passed with 10/10 tests on API 36, and final `adb devices` was empty after stopping `emulator-5564`.
- Latest solver contract regression guard: `LevelSolver.findPathFromSelection()` now has explicit unit coverage for already-complete valid selections and for rejecting out-of-board, repeated and non-adjacent selected paths before search. `tools/verify_release.py` gates these test markers.
- Latest level-scope regression guard: generated levels now have explicit unit coverage for the documented v1 difficulty bands, board sizes and canonical path lengths across all 36 levels. The model also rejects oversized 7x7 boards so released boards stay in the readable 4x4-6x6 range, and `tools/verify_release.py` gates these markers.
- Latest release-level content gate: `tools/verify_release.py` now requires the exact release-level unit markers for `createsThirtySixPlayableLevels`, the ordered id set `1..36`, canonical solution sum 56, adjacent canonical paths and independent solver reachability on every generated board.
- Latest responsive action bar hardening: gameplay controls now split into two visible-label rows on compact widths, and the landscape control column scrolls instead of clipping when font scale or height is constrained. Follow-up `./gradlew test lint assembleDebug assembleDebugAndroidTest bundleRelease`, `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` and `./tools/verify_release.py` passed; connected finished 10/10 tests on API 36.
- Latest ImageGen icon replacement and store screenshot recapture: the launcher/store icon now uses a cleaner generated raster puzzle mark (`play_store/source_assets/icon_imagegen_20260606.png` -> `play_store/icon/play_icon_512.png` and `app/src/main/res/drawable-nodpi/ic_launcher_imagegen.png`). Rejected launcher/store icons were archived as `play_store/archive/icon_imagegen_20260605_rejected.png` and `play_store/archive/icon_imagegen_20260606_rejected_owner_review.png`. After the icon resource change, all five phone screenshots and all five large/tablet screenshots were recaptured from the real release app on `Medium_Phone_API_35_Default`; each file is newer than the UI/theme/resource inputs, RGB/24-bit and Play-sized. `play_store/feature_graphic.png` was rebuilt from the refreshed `phone/04_game_line.png` crop.
- Latest final gate after ImageGen icon replacement: `tools/run_final_local_gate.py` now cleans generated connected-test outputs before optional connected execution, then `./tools/run_api36_connected_gate.py` passed end-to-end with `final_local_gate_ok` and `api36_connected_gate_ok`. The run covered `test lint assembleDebug bundleRelease`, API 36 connected 10/10, `release_verification_ok`, upload packet, Play Console packet, local privacy-policy HTML and signing-backup input checks; project-owned emulators were stopped afterward, while unrelated external emulators were not touched.
- Latest publication-readiness handoff hardening: `tools/print_publication_readiness.py` now separates local release-candidate readiness from production rollout readiness, prints `publication_readiness_local_ready_external_pending` during normal local preflight and fails under `--require-production-ready` while owner-controlled Play Console, privacy URL, support contact, signing backup and testing evidence remain unresolved.
- Latest brand-mark consistency hardening: onboarding now reuses `app/src/main/res/drawable-nodpi/ic_launcher_imagegen.png` through `painterResource(R.drawable.ic_launcher_imagegen)` instead of a separate hard-coded Compose `56` tile, so launcher, splash, store icon and first-run brand mark stay visually aligned. `tools/verify_release.py` gates this source marker.
- Latest onboarding screenshot polish: portrait onboarding now starts with a bounded responsive top spacer instead of centering the full rules stack on tall screens, so `phone/01_onboarding.png` has less empty top space while keeping the first-run instructions and CTA visible. Phone/tablet screenshots were recaptured and `play_store/upload_checksums.md` was refreshed.
- Latest final release smoke check after onboarding/store screenshot polish: production package `com.qgrid.mobile` installed on `Medium_Phone_API_36`; accessibility services were reset to normal release-smoke state (`accessibility_enabled=0`, `touch_exploration_enabled=0`), `com.qgrid.mobile/.MainActivity` cold-launched in 189 ms, UIAutomator exposed `Линия 56` and `Начать`, crash buffer had no matching app entries, and `docs/qa_artifacts/release_smoke_onboarding.png` was captured as visual evidence.
- Latest offline release smoke check: release package `com.qgrid.mobile` was installed, cleared and launched on `Medium_Phone_API_35_Default` with `airplane-mode enabled`, `airplane_mode_on=1`, `Wi-Fi is disabled`, `SSID: <unknown ssid>`, `IP: null` and `Supplicant state: DISCONNECTED`. UIAutomator verified onboarding `Линия 56`/`Начать`, home `Прогресс` and the level-1 game board with `Цель` plus `Клетка`; crash buffer had no matching app entries, and `docs/qa_artifacts/offline_release_smoke_onboarding.png` plus `docs/qa_artifacts/offline_release_smoke_game.png` were captured.
- Latest Play screenshot aspect-ratio and system-bar hardening: phone screenshots are now exported as 1080x2064 RGB/24-bit PNGs cropped from the real 1080x2400 release captures, and tablet screenshots are exported as 1600x2336 cropped from real 1600x2560 captures. The long side remains no more than 2x the short side, system status/navigation bars are removed from upload images, `tools/capture_store_screenshots.py` applies these crops on future recaptures, and `tools/verify_release.py` gates the general Google Play screenshot size/ratio rule instead of only fixed filenames.
- Latest connected-evidence gate hardening: `tools/verify_release.py` now requires a fresh `Medium_Phone_API_36(AVD) - 16` connected XML report for the 10-test suite, validates 0 failures/errors/skips and expected testcase names, and fails if the report is older than app source, androidTest source or Gradle build files.
- Latest build-artifact freshness gate hardening: `tools/verify_release.py` now requires `app-debug.apk` and `app-release.aab` to be newer than app/build inputs and the local SDK input (`local.properties` when present), and the release AAB also has to be newer than ProGuard plus upload signing inputs.
- Latest artifact-ignore hygiene hardening: `.gitignore` and `tools/verify_release.py` now cover copied Android upload/install artifacts (`*.apk`, `*.aab`, `*.apks`, `*.idsig`) in addition to generated build directories, signing files and local property files.
- Latest AGENTS handoff hardening: `AGENTS.md` now lists the full verifier-required documentation and Play handoff set, including Google Play sources, completion/rejected-asset docs, App content answers and screenshot manifest. `tools/verify_release.py` gates the project contract through `check_agents_handoff`.
- Latest signing-file hygiene hardening: `tools/verify_release.py` now checks every existing `private/signing/*.p12` file for non-group/non-world-readable permissions and requires every local signing file to appear only in the Do Not Upload section of `play_store/upload_manifest.md`. Any stale ignored legacy signing file is documented as not used and not uploadable.
- Latest owner-input handoff hardening: `play_store/owner_release_inputs.md` now isolates support contact, public privacy URL, signing backup, account testing path, generated-artifact review, fixed local decisions and items that must not change without rebuilding/rechecking. `tools/verify_release.py` now gates this handoff through `check_owner_release_inputs`.
- Latest release-plan handoff hardening: `tools/verify_release.py` now gates `docs/release_plan.md` through `check_release_plan_handoff`, including owner-input resolution, internal/closed testing order, public HTTPS privacy hosting, signing backup, signing env-var fallback, Play handoff files and final verification commands.
- Latest tech-stack handoff hardening: `docs/tech_stack_decision.md` now records exact Android/Kotlin/Compose/SDK versions, neutral package identity, architecture boundaries, dependency limits, no-network/no-data constraints, release signing inputs and local verification commands. `tools/verify_release.py` gates the document through `check_tech_stack_handoff`.
- Latest Play app-content/content-rating handoff hardening: `play_store/content_rating_notes.md` now documents the expected Games/Puzzle rating posture, non-child-directed target-audience alignment and local no-network/no-risk evidence. `tools/verify_release.py` now gates `play_store/app_content_answers_ru.md` and `play_store/content_rating_notes.md` through focused Play Console answer markers.
- Latest completion-audit handoff hardening: `tools/verify_release.py` now gates this completion audit for proven local completion evidence, latest hardening entries, remaining external publication blockers and the current verdict.
- Latest release-report handoff hardening: `tools/verify_release.py` now gates `docs/release_report.md` through `check_release_report_handoff`, including selected idea, neutral package, implemented core loop, privacy posture, Play handoff files, current 10/10 API 36 connected evidence, build artifacts, owner-input handoff and remaining manual Play gates.
- Latest Google Play checklist handoff hardening: `tools/verify_release.py` now gates `docs/google_play_checklist.md` through `check_google_play_checklist_handoff`, including listing copy, package/build setup, signing backup, verification commands, API 36 connected evidence, Play assets, privacy policy, data safety, permissions, content rating, target audience, testing instructions, known limitations and manual Play Console actions.
- Latest QA test-plan handoff hardening: `tools/verify_release.py` now gates `docs/qa_test_plan.md` through `check_qa_test_plan_handoff`, including unit coverage areas, instrumentation scenarios, compact/font-scale checks, landscape/tablet checks, accessibility/TalkBack evidence, release smoke, permissions/AAB cleanliness gates, current 10/10 API 36 connected evidence and remaining manual screenshot recapture guidance.
- Latest discreet package gate hardening: `tools/verify_release.py` now separately enforces neutral production/debug package identity, Kotlin package declarations, generated debug/release manifest package names and release AAB absence of old personal/obvious package markers.
- Latest product-spec/content-audit handoff hardening: `tools/verify_release.py` now gates `docs/product_spec.md` and `docs/content_audit.md` through focused markers for selected gameplay scope, hints, progress, privacy, localization, content completeness and owner-review risk.
- Latest UI/accessibility/privacy handoff hardening: `tools/verify_release.py` now gates `docs/ui_audit.md`, `docs/accessibility_notes.md` and expanded `docs/privacy_and_permissions.md` markers for responsive layout evidence, readable controls, result/back/motion/haptics polish, TalkBack evidence, local-only state, disabled backup and blocked data/network dependency surfaces.
- Latest asset/art-direction handoff hardening: `tools/verify_release.py` now gates `docs/art_direction.md` and `docs/asset_prompts.md` through markers for the connected-path visual identity, readable no-text icon, real-screenshot feature graphic composition, no fake UI, no noisy/mascot direction, no promotional text and no copied brands/characters/UI.
- Latest screenshot-manifest handoff hardening: `tools/verify_release.py` now parses `play_store/screenshots/manifest.md` and requires exactly the five phone and five large/tablet release-ready screenshot rows, real release-app capture markers, package/format capture notes and recapture guidance after visual or Play screenshot-size changes.
- Latest neutral package and navigation evidence refresh: production package remains `com.qgrid.mobile`, debug package remains `com.qgrid.mobile.debug`, and `LevelNavigation` plus `AppUiState` now de-duplicate available ids before direct/next-level routing, progress totals and all-complete result state. Follow-up `./gradlew test lint bundleRelease`, `ANDROID_SERIAL=emulator-5564 ./gradlew connectedDebugAndroidTest` and `./tools/verify_release.py` passed; connected finished 10/10 tests on API 36 and final `adb devices` was empty.
- Latest result replay hardening: win result panel now exposes `Повторить`, and `GameViewModel.replayCurrentLevel()` starts a clean same-level game without clearing completed progress. Connected coverage `completedResultCanReplayCurrentLevel` proves the replay path through the real Compose UI.
- Latest privacy-policy placeholder-removal hardening: `play_store/privacy_policy_ru.md` and `play_store/privacy_policy_ru.html` no longer contain a contact-replacement block. The policy uses the Google Play listing support contact as its privacy inquiry mechanism, and `tools/verify_release.py` now fails if the publication policy source regresses to `contact-required`, replacement instructions, placeholder text, or a missing inquiry mechanism. Follow-up `python3 -m py_compile tools/verify_release.py tools/capture_store_screenshots.py`, `./tools/verify_release.py` and `./gradlew test lint assembleDebug bundleRelease` passed.
- Latest empty-level UI-state guard: `AppUiState.shouldShowContinueAction` now requires `hasLevels`, preventing an accidental empty level list from showing a false continue route. Unit coverage `emptyLevelListDoesNotExposeContinueAction` and `tools/verify_release.py` gate this regression guard. Follow-up `./gradlew test lint assembleDebug bundleRelease`, `ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest` on `Medium_Phone_API_36(AVD) - 16` and `./tools/verify_release.py` passed.
- Latest instrumentation state isolation hardening: `ProgressRepository.clearLocalState()` and `Line56AppSmokeTest.resetLocalStateBeforeEachTest()` now make connected tests independent from prior saved progress/settings while preserving per-test persistence assertions. `tools/verify_release.py` gates these markers. Follow-up `./gradlew test lint assembleDebug bundleRelease`, `ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest` on `Medium_Phone_API_36(AVD) - 16` and `./tools/verify_release.py` passed.
- Latest home progress accessibility hardening: the home progress indicator now has localized content description plus `ProgressBarRangeInfo`, and connected coverage checks the clean first-home `Прогресс: 0 из 36 уровней.` semantics. `tools/verify_release.py` gates these source/test markers. Follow-up `./gradlew test lint assembleDebug bundleRelease`, `ANDROID_SERIAL=emulator-5556 ./gradlew connectedDebugAndroidTest` on `Medium_Phone_API_36(AVD) - 16` and `./tools/verify_release.py` passed.
- Latest launcher ImageGen cleanup hardening: the old unused vector launcher foreground was removed from Android app resources, the adaptive/fallback launcher XML files are verifier-gated to the ImageGen raster foreground, and `tools/verify_release.py` now fails if `app/src/main/res/drawable/ic_launcher_foreground.xml` returns. Follow-up `./gradlew test lint assembleDebug bundleRelease`, `ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest` on `Medium_Phone_API_36(AVD) - 16` and `./tools/verify_release.py` passed; connected finished 10/10 tests with 0 skipped and 0 failed tests.
- Latest packaged launcher asset gate hardening: the release verifier now proves the Android source launcher PNG and Google Play icon PNG are byte-for-byte identical, checks both as 512x512 opaque-alpha PNGs and inspects the signed release AAB for packaged `ic_launcher_imagegen.png` with no old `ic_launcher_foreground` entry. Follow-up `python3 -m py_compile tools/verify_release.py tools/capture_store_screenshots.py` and `./tools/verify_release.py` passed.
- Latest store screenshot automation hardening: the Play screenshot capture tool now uses fresh `adb exec-out uiautomator dump /dev/tty` XML, rejects UI trees not owned by `com.qgrid.mobile`, and keeps deterministic coordinate fallbacks for known capture controls on the fixed phone/tablet viewports so polluted local AVDs cannot reuse stale XML from another app.
- Latest store screenshot crop-validation hardening: `tools/capture_store_screenshots.py` now validates Play screenshot crop boxes before writing upload PNGs, and `tools/verify_release.py` proves the phone/tablet crop boxes produce the documented upload sizes while rejecting negative, empty and out-of-bounds crop boxes.
- Latest saved store screenshot PNG validation hardening: `tools/capture_store_screenshots.py` now reopens each saved capture and rejects missing files, non-PNG output, wrong saved dimensions and alpha/non-RGB PNGs before checksum updates. `tools/verify_release.py` covers the valid RGB PNG path plus wrong-size, alpha-PNG and JPEG negative cases.
- Latest feature graphic rebuild validation hardening: `tools/capture_store_screenshots.py` now defines explicit feature-graphic size/crop/panel constants, validates the gameplay crop with the shared crop guard and reopens the rebuilt `play_store/feature_graphic.png` as a 1024x500 RGB PNG before checksum updates. `tools/verify_release.py` proves the crop size and rejects too-small feature source images.
- Latest store screenshot checksum-update hardening: `tools/capture_store_screenshots.py` now validates `play_store/upload_checksums.md` before rewriting it after a recapture, rejecting malformed checksum rows, duplicate upload rows, missing required rows, unexpected upload rows and wrong row order. `tools/verify_release.py` proves each negative case through direct helper calls.
- Latest home progress visual polish: the home progress bar disables the Material3 stop indicator so a clean `0 / 36` home state shows only the neutral track, not a misleading dark end dot. Phone and large/tablet Play screenshots were recaptured from the real release app and the upload checksum manifest was refreshed.
- Latest continuation audit on 6 June 2026: the original RTF instruction, current `AGENTS.md`, requirement traceability and owner-input handoff were re-read against the current worktree. Official Google Play / Android Developers source pages were spot-checked again for target API, AAB upload, preview assets, Data Safety, User Data/privacy policy, content rating, target audience and personal-account closed testing. Local ignored signing files were checked for presence, `.gitignore` coverage and owner-only permissions; `ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest` passed 10/10 on `Medium_Phone_API_36(AVD) - 16`; `./tools/verify_release.py` returned `release_verification_ok`; final cleanup left `adb devices` empty.
- Latest upload-runbook handoff hardening: `play_store/upload_runbook_ru.md` now consolidates the owner upload-day sequence: local preflight, owner inputs, app identity, App content/policy sources, exact upload assets, Do Not Upload list, release-track order, stop conditions and post-upload evidence to record. `tools/verify_release.py` gates the runbook.
- Latest final local gate runner hardening: `tools/run_final_local_gate.py` now runs the full owner handoff preflight in order and prints `final_local_gate_ok` only after the Gradle build/test gate, release verifier, upload packet, post-upload evidence packet, closed-testing evidence dry run, privacy/contact evidence packet, Play Console forms evidence packet, Play pre-launch review evidence packet, store asset review sheet dry run, store-listing preview evidence packet, upload archive dry run, Play Console packet, publication-readiness check, Play-generated APK verification dry run, Play-generated APK evidence dry run, local privacy HTML check, signing input check and signing-backup evidence packet all pass. `tools/verify_release.py` gates the runner source and dry-run command list.
- Latest final local gate execution: `./tools/run_final_local_gate.py --include-hosted-privacy` passed with `final_local_gate_ok`, covering `./gradlew test lint assembleDebug assembleRelease bundleRelease`, `release_verification_ok`, `upload_packet_ok`, `post_upload_evidence_packet_ok`, `closed_testing_evidence_packet_dry_run_ok`, `privacy_contact_evidence_packet_ok`, `play_console_forms_evidence_packet_ok`, `pre_launch_review_evidence_packet_ok`, `store_asset_review_sheet_dry_run_ok`, `store_listing_review_evidence_packet_ok`, `play_upload_archive_dry_run_ok`, `play_upload_archive_existing_ok`, `play_console_packet_ok`, `publication_readiness_local_ready_external_pending`, `play_generated_apk_verify_dry_run_ok`, `play_generated_apk_evidence_packet_dry_run_ok`, `privacy_policy_local_ok`, `signing_backup_input_ok` and `signing_backup_evidence_packet_ok`. Production rollout remains externally gated until owner evidence makes `./tools/print_publication_readiness.py --require-production-ready` pass.
- Latest upload-packet helper hardening: `tools/print_upload_packet.py` now verifies the current upload checksum manifest against the real signed AAB and Play assets, rejects secret/debug/test artifacts in the upload packet, and prints a safe owner-facing upload list plus Do Not Upload list. `tools/verify_release.py` gates the helper and handoff markers.
- Latest upload-packet negative-regression gate: `tools/verify_release.py` now imports `tools/print_upload_packet.py` and proves the upload packet validator rejects unsafe checksum paths, duplicate checksum rows, forbidden upload paths, wrong byte counts, wrong SHA-256 values, paths missing from `play_store/upload_manifest.md` and a manifest without a Do Not Upload section.
- Latest upload-packet completeness hardening: `tools/print_upload_packet.py` now requires the exact ordered upload path set for the signed AAB, Play icon, feature graphic, five phone screenshots and five large/tablet screenshots before printing `upload_packet_ok`. `tools/verify_release.py` proves the helper rejects missing required upload paths, unexpected upload paths and wrong upload-path order.
- Latest upload checksum row-format hardening: `tools/print_upload_packet.py` and `tools/prepare_play_upload_archive.py` now parse checksum rows defensively and reject non-numeric/negative byte counts plus malformed or uppercase SHA-256 values with owner-facing helper errors. The archive helper also rejects duplicate checksum rows during parsing. `tools/verify_release.py` proves these malformed-row cases for both helpers.
- Latest store asset review sheet hardening: `tools/create_store_asset_review_sheet.py` now validates Play icon, feature graphic and phone/tablet screenshot dimensions, and generated `play_store/store_asset_review_sheet.png`, 1800x2050 RGB/24-bit PNG, 733,432 bytes. The sheet is internal owner evidence for local crop review and is excluded from Play uploads.
- Latest store asset review sheet negative-regression gate: `tools/verify_release.py` now imports `tools/create_store_asset_review_sheet.py` and proves the helper rejects missing assets, wrong image dimensions, grayscale PNGs and non-PNG assets before `store_asset_review_sheet_ok` can be trusted.
- Latest masked icon preview freshness gate: `tools/verify_release.py` now requires `play_store/icon/play_icon_preview_masked.png` to be newer than the current full-square Play icon and to contain transparent mask pixels, preventing stale or unmasked local icon-preview evidence after future ImageGen icon changes.
- Latest Play upload archive helper hardening: `tools/prepare_play_upload_archive.py` now verifies the upload checksum manifest and can create a generated owner handoff ZIP under `build/play_upload` with only the verified AAB/assets plus safe handoff notes. The final local gate includes the helper's dry run, and the generated ZIP is explicitly not a Play Console upload artifact.
- Latest Play upload archive negative-regression gate: `tools/verify_release.py` now imports `tools/prepare_play_upload_archive.py` and proves the helper rejects unsafe upload/handoff paths, forbidden signing/debug source paths, rows missing from `play_store/upload_manifest.md`, wrong byte counts, wrong SHA-256 values, missing handoff files, duplicate archive entries and forbidden archive entry names.
- Latest Play upload archive completeness hardening: `tools/prepare_play_upload_archive.py` now requires the same exact ordered upload path set as the owner upload-packet helper before dry-run/write can succeed. `tools/verify_release.py` proves the archive helper rejects missing required archive paths, unexpected archive paths and wrong archive-path order.
- Latest generated upload archive freshness gate: `tools/verify_release.py` now validates the existing `build/play_upload/line56_v1_google_play_upload_packet.zip` when present: exact entry set, current upload-file bytes and SHA-256 values, current safe handoff-file bytes, forbidden path markers, ZIP size budget, README warnings and regeneration freshness after archive helper/upload/handoff changes.
- Latest Play upload archive generation: `./tools/prepare_play_upload_archive.py --write` created `build/play_upload/line56_v1_google_play_upload_packet.zip`, 4,357,875 bytes, SHA-256 `c5ba9f695d4ebd54ed43d28dafdb98fc617570af5370d86a110e86a9e5745651`; ZIP inspection found 33 entries and no forbidden signing/debug/test/source-asset path markers.
- Latest signing handoff archive inclusion: generated owner ZIP now includes safe `_owner_handoff/signing_backup_evidence_ru.md` and `_owner_handoff/signing_certificate_report.md` alongside upload/runbook/privacy/policy notes, so the upload-day packet contains the signing-backup template and public certificate fingerprints without including signing secrets.
- Latest generated upload archive existing-verification hardening: `tools/prepare_play_upload_archive.py --verify-existing` now verifies the generated owner ZIP itself against current upload assets, current handoff notes, expected entries, deterministic timestamps/modes, README text and forbidden entry markers; `tools/run_final_local_gate.py` includes this check after the archive dry run.
- Latest Play Console packet helper hardening: `tools/print_play_console_packet.py` now verifies listing/App content/Data Safety/content-rating/owner-gate handoff consistency and prints a safe copy-ready Play Console form packet. `tools/verify_release.py` gates the helper and output markers.
- Latest Play Console packet negative-regression gate: `tools/verify_release.py` now imports `tools/print_play_console_packet.py` and proves the listing validator rejects an app name over 30 characters, a short description over 80 characters, a full description over 4000 characters, release notes over 500 characters, placeholder markers and a listing/submission handoff mismatch before the copy-ready Play Console packet can be accepted.
- Latest Play-generated APK verification helper hardening: `tools/verify_play_generated_apk.py` now gives the owner a post-upload command for downloaded Play-generated APK artifacts. It verifies package `com.qgrid.mobile`, version `1`/`1.0.0`, label `Линия 56`, minSdk/targetSdk `24`/`36`, APK signature validity, signer certificate SHA-256, no Android Debug certificate, no `INTERNET`, no `ACCESS_NETWORK_STATE`, no dangerous runtime permissions, `allowBackup=false`, no debuggable release manifest, `extractNativeLibs=false`, no debug/test ZIP markers, native 16 KB ELF `PT_LOAD` alignment, uncompressed 16 KB ZIP-aligned native libraries, a 512x512 PNG icon candidate whose pixels match `play_store/icon/play_icon_512.png`, an application icon reference linked to that matching PNG and a round icon reference linked to the same matching PNG.
- Latest Play-generated APK verification negative-regression gate: `tools/verify_release.py` now runs `./tools/verify_play_generated_apk.py --dry-run`, proves the local release APK passes with `play_generated_apk_verify_ok`, proves signature/debug-certificate regressions are rejected, proves manifest privacy regressions are rejected, proves a temporary APK with a mismatched 512x512 icon PNG is rejected, proves unlinked application/round icon matches are rejected and proves the debug APK is rejected because it is `com.qgrid.mobile.debug`.
- Latest Play-generated manifest privacy owner-evidence hardening: `tools/verify_play_generated_apk.py` now prints `allowBackup: false` and `debuggable: absent` or `debuggable: false` for reviewed APKs; `tools/print_publication_readiness.py` requires owner evidence to mention `allowBackup=false` and `no debuggable` before production readiness can be confirmed.
- Latest Play-generated icon pixel-match helper hardening: `tools/verify_play_generated_apk.py` now compares RGBA pixels for APK 512x512 icon candidates against `play_store/icon/play_icon_512.png`, verifies the application and round icon XML/resource-table paths link to that matching PNG, and prints `store icon pixel matches: ...`, `application icon linked store icon: ...` and `round icon linked store icon: ...`; `tools/print_publication_readiness.py` requires owner evidence for the Play-generated icon field to mention application-icon-linked and round-icon-linked store-icon pixel matches instead of accepting a bare `yes`.
- Latest post-upload evidence handoff hardening: `play_store/play_console_post_upload_evidence_ru.md` now gives the owner a safe external-evidence template for Play Console upload facts, privacy URL/contact status, generated artifact review, policy forms, testing tracks, pre-launch result, policy warnings and stop-release notes without storing secrets or tester personal data.
- Latest privacy-policy URL helper hardening: `tools/check_privacy_policy_url.py` now checks the local privacy HTML and gives the owner a URL-mode command for the final public HTTPS privacy-policy URL; `tools/verify_release.py` gates local mode.
- Latest hosted privacy policy exact-content hardening: `tools/check_privacy_policy_url.py --url <https-url>` now rejects URLs with credentials, non-public hosts, non-HTTPS redirects, PDF final paths, invalid UTF-8 and script/tracker/widget markers, and requires the hosted normalized text to match `play_store/privacy_policy_ru.html`. Local mode prints the canonical privacy text SHA-256 for owner comparison before hosting.
- Latest privacy helper negative-regression gate: `tools/verify_release.py` now imports `tools/check_privacy_policy_url.py`, verifies the printed canonical text SHA-256, validates the local policy through the helper API, and proves the helper rejects a script-injected HTML variant plus a hosted-text mismatch against `play_store/privacy_policy_ru.html`.
- Latest privacy URL public-host hardening: `tools/check_privacy_policy_url.py --url <https-url>` now rejects credentialed URLs, URL fragments, placeholder/reserved hosts, PDF paths and domains that fail DNS or resolve to non-global/private IP addresses before the hosted policy can be accepted. `tools/verify_release.py` proves these URL negative cases with direct helper calls, including a mocked private DNS resolution.
- Latest privacy URL query-parameter hardening: `tools/check_privacy_policy_url.py` and `tools/print_publication_readiness.py` now reject privacy policy URLs that include query parameters, so Play Console handoff requires a clean stable HTTPS policy URL with no credentials, query string or fragment.
- Latest signing-backup evidence hardening: `tools/check_signing_backup_inputs.py` checks the ignored local upload keystore and `keystore.properties` before owner backup without printing password values, and `play_store/signing_backup_evidence_ru.md` records the safe 6 June 2026 local preflight result while keeping the actual secure backup as owner-controlled evidence. `tools/verify_release.py` gates the helper, local evidence, template fields and handoff markers.
- Latest signing-backup negative-regression gate: `tools/verify_release.py` now imports `tools/check_signing_backup_inputs.py` and proves the signing preflight rejects group/world-readable signing files, missing required keystore properties, a legacy/wrong `storeFile` and a wrong signing alias before `signing_backup_input_ok` can be trusted.
- Latest signing-backup evidence packet hardening: `tools/print_signing_backup_evidence_packet.py` now validates the local signing inputs without printing password values, prints safe owner backup evidence lines for `play_store/signing_backup_evidence_ru.md`, prints matching post-upload backup lines for `play_store/play_console_post_upload_evidence_ru.md`, supports `--backup-date <date/time>` after the real owner-controlled backup, and is included in `tools/run_final_local_gate.py` plus `tools/verify_release.py`.
- Latest signing-backup completion evidence hardening: `tools/print_publication_readiness.py` now rejects bare `yes` for `Backup completed before Play upload`; owner evidence must explicitly mention `private/signing/qgrid-upload.p12`, `keystore.properties` and `before Play upload`.
- Latest signing-backup storage evidence hardening: `tools/print_publication_readiness.py` now rejects vague storage-type evidence; owner evidence must explicitly mention `owner-controlled`, `secure` and a concrete storage type such as password manager or encrypted offline backup.
- Latest signing-backup owner-record evidence hardening: `tools/print_publication_readiness.py` now rejects vague responsible-owner and backup-record-location evidence; owner evidence must use a role-based release-owner reference in an owner tracker and a backup record location in an owner tracker or password manager without storage access details.
- Latest active-keystore backup evidence hardening: `tools/print_publication_readiness.py` now rejects bare `yes` for the post-upload active-keystore backup field and requires evidence that explicitly mentions `private/signing/qgrid-upload.p12` and `before AAB upload`.
- Latest post-upload signing-evidence alignment: `play_store/play_console_post_upload_evidence_ru.md` now references the already recorded local signing preflight and keeps only the actual owner-controlled backup/upload facts as unavailable locally.
- Latest connected-test stability hardening: `Line56AppSmokeTest.waitForLevelBoard()` now retries through a transient missing Compose hierarchy during screen transitions. Follow-up `ANDROID_SERIAL=emulator-5560 ./gradlew connectedDebugAndroidTest` passed 10/10 on `Medium_Phone_API_36(AVD) - 16`, `./tools/verify_release.py` passed, `./tools/run_final_local_gate.py` returned `final_local_gate_ok`, and final cleanup left `adb devices` empty.
- Latest connected-artifact identity hardening: `tools/verify_release.py` now requires the fresh connected XML/textproto to identify `com.qgrid.mobile.Line56AppSmokeTest` and `com.qgrid.mobile`, and scans text connected artifacts for stale package markers from earlier project identities.
- Latest optional connected final-gate hardening: `tools/run_final_local_gate.py --include-connected --connected-serial <serial>` now refreshes `connectedDebugAndroidTest` evidence before the verifier in one owner-facing preflight command, while the default final local gate remains usable without a live emulator.
- Latest dirty-emulator connected cleanup hardening: `tools/run_final_local_gate.py --include-connected --connected-serial <serial>` now force-stops/kills known stale local package processes, removes their installed debug/test packages from the selected dirty emulator, verifies that stale packages/processes are gone before instrumentation starts, and prevents older local test packages or windows from stealing focus and leaving `No compose hierarchies found` failures in current connected evidence.
- Latest optional connected final-gate execution: `./tools/run_final_local_gate.py --include-connected --connected-serial emulator-5560` passed on `Medium_Phone_API_36(AVD) - 16`; connected evidence was refreshed before the verifier, 10/10 connected tests passed, and the full helper returned `final_local_gate_ok`.
- Latest managed API 36 connected-gate helper hardening: `tools/run_api36_connected_gate.py` now automates the full API 36 connected preflight while preserving emulator ownership boundaries: it starts a clean `Medium_Phone_API_36` on `emulator-5560` with `-wipe-data`, retries the cleaned AVD once without `-wipe-data` if the emulator exits after the wipe reset before boot, runs the connected final gate, retries that connected final gate once with a freshly booted cleaned AVD if the managed emulator is lost after boot, and stops only the emulator it started. This prevents stale debug/test APKs from older local projects from stealing focus during connected evidence refreshes while giving transient ADB/emulator loss one clean recovery path.
- Latest connected start-state isolation hardening: `Line56AppSmokeTest.resetLocalStateBeforeEachTest()` now clears DataStore, marks onboarding as seen and returns to home without forcing Activity recreation in per-test setup; `ensureHome()` handles onboarding text and nested back navigation before asserting `Прогресс`. This keeps connected tests order-independent while avoiding AndroidX `InstrumentationActivityInvoker` cleanup crashes on clean API 36 AVDs. The dedicated persistence test still verifies Activity recreation after a win, and the follow-up `ANDROID_SERIAL=emulator-5554 ./gradlew connectedDebugAndroidTest` passed 10/10 on `Medium_Phone_API_36(AVD) - 16`.
- Latest clean API36 final evidence refresh: `ensureHome()` now uses the Activity back dispatcher for nested Levels/Result/Settings/About screens, and the fresh connected XML records 10 tests, 0 failures, 0 errors and 0 skipped on `Medium_Phone_API_36(AVD) - 16`. Follow-up `./tools/verify_release.py` returned `release_verification_ok` and `./tools/run_final_local_gate.py` returned `final_local_gate_ok`; production rollout remains externally gated by Play Console, privacy URL and owner signing/testing evidence.
- Latest current-package connected cleanup hardening: `tools/run_final_local_gate.py --include-connected --connected-serial <serial>` now removes the current local debug app package `com.qgrid.mobile.debug` and instrumentation package `com.qgrid.mobile.debug.test` before `connectedDebugAndroidTest`, in addition to the legacy local package ids, so a dirty emulator cannot reuse a stale current debug/test install.
- Latest production-package connected focus cleanup hardening: `tools/run_final_local_gate.py --include-connected --connected-serial <serial>` now force-stops the current production package `com.qgrid.mobile` before instrumentation, without uninstalling it, so a release-smoke app window left open on the selected emulator cannot steal focus from the debug connected test.
- Latest owner backup-evidence specificity hardening: production readiness now requires explicit safe backup evidence. `tools/print_publication_readiness.py` rejects vague owner evidence that does not say backup evidence was recorded without secrets, does not explicitly mention two owner-controlled secure copies or does not say recovery was tested without exposing secrets; `tools/verify_release.py` gates these negative cases.
- Latest owner evidence validation hardening: `tools/print_publication_readiness.py` now validates recorded owner evidence values against exact release expectations before production-ready output is possible: package `com.qgrid.mobile`, version `1`/`1.0.0`, signed AAB SHA-256 from `play_store/upload_checksums.md`, public HTTPS privacy URL shape, Play Console no-data/no-ads/no-account policy posture, Play-generated no-permission, manifest privacy and native 16 KB page-size review, testing/pre-launch status and signing-backup evidence. Current output remains `publication_readiness_local_ready_external_pending` because external owner gates are still unresolved.
- Latest publication-readiness negative-regression gate: `tools/verify_release.py` now imports `tools/print_publication_readiness.py` and proves the owner-evidence validators reject a wrong uploaded package, wrong AAB SHA-256, non-HTTPS privacy URL, incomplete no-permissions review, recorded signing secret marker and wrong signing alias before production-ready output can be confirmed.
- Latest privacy URL evidence alignment hardening: `tools/print_publication_readiness.py` now rejects recorded privacy-policy URLs with credentials, query parameters, placeholder hosts, private/local IPs or URL fragments, and requires `--privacy-url <https-url>` to match the recorded `Public privacy policy URL` before the external URL check can count toward production readiness. `tools/verify_release.py` proves these negative cases, including a mismatched checked URL.
- Latest recorded privacy URL pre-upload hardening: `tools/print_publication_readiness.py --check-recorded-privacy-url` now validates the hosted privacy URL recorded in `play_store/play_console_post_upload_evidence_ru.md`, normalizes sentence punctuation before invoking the URL checker, and `tools/run_final_local_gate.py --include-hosted-privacy` exposes that check as an explicit networked upload-day preflight while keeping the default final local gate offline-safe. `tools/verify_release.py` proves the command wiring and trailing-period normalization.
- Latest managed hosted-privacy preflight hardening: `tools/run_api36_connected_gate.py --include-hosted-privacy` now passes the recorded hosted privacy URL check through the project-owned API 36 emulator flow, so the managed upload-day command can refresh connected evidence and validate the live privacy policy in one run.
- Latest remote handoff verification hardening: `tools/verify_remote_release.py` provides a networked post-push check for `origin/main` and `origin/gh-pages`: remote release branch matches local `HEAD`, every remote upload asset in `play_store/upload_checksums.md` matches its recorded bytes and SHA-256, forbidden signing/install artifacts are absent from remote trees, the privacy HTML exists on the pages branch and the recorded hosted privacy URL still returns `privacy_policy_url_ok`.
- Latest remote upload-asset checksum hardening: `tools/verify_remote_release.py` now verifies the remote signed AAB, store icon, feature graphic, five phone screenshots and five large/tablet screenshots from `play_store/upload_checksums.md`, prints the verified upload-asset count and rejects any remote upload asset byte/SHA mismatch before the GitHub handoff can be accepted.
- Latest annotated remote tag handoff hardening: `tools/verify_remote_release.py --tag <release-tag>` now fetches the release tag into a remote-tracking ref and requires it to be an annotated tag that peels to local `HEAD`, so a pushed GitHub RC tag cannot silently point at an older handoff commit or be reduced to a lightweight tag while `origin/main` is current.
- Latest remote signing-artifact extension hardening: `tools/verify_remote_release.py` now rejects additional signing-key path extensions in fetched GitHub trees, including `.jks`, `.keystore`, `.pem`, `.pk8` and `.key`, in addition to the existing `.p12`, private-directory and install-artifact checks.
- Latest remote unexpected-AAB hardening: `tools/verify_remote_release.py` now rejects any extra remote `.aab` files outside the expected signed upload artifact path `app/build/outputs/bundle/release/app-release.aab`, so a copied bundle cannot be pushed to GitHub without failing post-push release verification.
- Latest native 16 KB page-size verifier hardening: `tools/verify_release.py` now parses ELF program headers in the signed AAB and requires every native `.so` `PT_LOAD` alignment to be at least 16,384 bytes; `tools/verify_play_generated_apk.py` applies the same ELF check to downloaded Play-generated APKs and additionally verifies uncompressed native-library ZIP packaging, 16 KB ZIP data alignment and `extractNativeLibs=false`.
- Latest Play-generated native owner-evidence hardening: `tools/print_publication_readiness.py` now requires safe owner evidence that the downloaded Play-generated APK native libraries support 16 KB page sizes, including explicit `16 KB`, `16384`, `uncompressed`, `ZIP-aligned` and `extractNativeLibs=false` wording, before production readiness can be confirmed.
- Latest Play-generated version owner-evidence hardening: `tools/print_publication_readiness.py` now rejects bare `yes` for Play-generated version review and requires explicit `versionCode 1` plus `versionName 1.0.0` evidence from the downloaded/generated APK review.
- Latest Play-generated install/launch evidence hardening: `tools/print_publication_readiness.py` now rejects bare `yes` for the Play-generated APK install/launch field and requires explicit wording that the downloaded APK was installed and launched on an Android device or Android emulator. `tools/verify_release.py` proves weak install/launch evidence cannot unlock production readiness.
- Latest Play-generated APK evidence packet hardening: `tools/print_play_generated_apk_evidence_packet.py` now verifies a downloaded Play-generated APK through `tools/verify_play_generated_apk.py`, requires explicit Play-generated and Android device/emulator install-launch confirmations, rejects secrets/private links and prints safe Play-Generated APK Review Lines for post-upload evidence. The final local gate uses only `--dry-run`.
- Latest Play store preview crop evidence hardening: `tools/print_publication_readiness.py` now rejects bare or partial store-listing preview evidence and requires the owner record to mention the icon, feature graphic, phone screenshots, tablet screenshots and no damaging crops. `tools/verify_release.py` proves weak crop-review evidence cannot unlock production readiness.
- Latest hosted privacy owner-gate wording alignment: release handoff docs now distinguish the hosted privacy URL, which is already recorded and verifier-checkable, from the still-external Play Console URL entry and support/contact fields. `tools/verify_release.py` gates the updated wording so owner checklists do not drift back to saying the public URL itself is missing.
- Latest Play Console policy-form evidence hardening: `tools/print_publication_readiness.py` now rejects bare `yes` for App access, ads, Data Safety, content rating, target audience and AI disclosure evidence. Owner evidence must explicitly mention no restricted access/login/account, no ads, no user data collected/shared, Games / Puzzle, 13+ non-child-directed posture and no in-app generative AI features.
- Latest Play support/contact evidence hardening: `tools/print_publication_readiness.py` now rejects bare `yes` for Play Console support/contact evidence. Owner evidence must explicitly say the Play Console support/contact field is populated and that the privacy policy uses the Google Play listing support contact.
- Latest Play support/contact inquiry-mechanism evidence hardening: `tools/print_publication_readiness.py` now rejects support/contact mechanism evidence that omits the privacy inquiry mechanism; owner evidence must explicitly say the privacy policy inquiry mechanism uses the Google Play listing support contact.
- Latest Play support/contact real-contact evidence hardening: `tools/print_publication_readiness.py` now rejects support/contact field evidence that only says a field was populated; owner evidence must explicitly say the Play Console support/contact field is populated with a real support contact for privacy inquiries.
- Latest Play support/contact type evidence hardening: `tools/print_publication_readiness.py` now rejects support/contact evidence that omits the safe contact type; owner evidence must say whether the real Play Console support/contact value is a support email or support website URL without recording the actual contact value.
- Latest Play support/contact value-redaction evidence hardening: `tools/print_publication_readiness.py` now rejects owner evidence that records the actual support email address or support website URL while still requiring the safe contact type.
- Latest privacy/contact handoff hardening: `play_store/privacy_contact_handoff_ru.md` now gives the owner a safe Play Console privacy URL and support/contact checklist with exact publication-readiness evidence phrases, while explicitly forbidding the actual support email or URL in repository evidence. `tools/print_play_console_packet.py`, `tools/prepare_play_upload_archive.py` and `tools/verify_release.py` gate the handoff and include it in the owner ZIP.
- Latest post-upload evidence packet hardening: `tools/print_post_upload_evidence_packet.py` now prints safe upload artifact identity and internal-testing evidence lines from the verified AAB checksum manifest, supports `--upload-date <date/time>` after the real Play Console upload, and is included in `tools/run_final_local_gate.py` plus `tools/verify_release.py`.
- Latest privacy/contact evidence packet hardening: `tools/print_privacy_contact_evidence_packet.py` now prints safe privacy URL and support/contact evidence lines for `play_store/play_console_post_upload_evidence_ru.md`, supports support-email and support-website evidence paths, rejects actual support email/URL and secret markers, and is included in `tools/run_final_local_gate.py` plus `tools/verify_release.py`.
- Latest Play Console forms evidence packet hardening: `tools/print_play_console_forms_evidence_packet.py` now prints safe App access, ads, Data Safety, content rating, target audience and AI disclosure evidence lines for `play_store/play_console_post_upload_evidence_ru.md` after the owner actually completes the matching Play Console forms, rejects secret/contact/tester URL markers in generated evidence, and is included in `tools/run_final_local_gate.py` plus `tools/verify_release.py`.
- Latest closed-testing evidence packet hardening: `tools/print_closed_testing_evidence_packet.py` now prints safe closed-testing and production-access evidence lines for both Play account paths. The final local gate uses `--dry-run`; after Play Console confirms the account path, the owner must use exactly one of `--not-required` or `--required-completed`. The helper rejects tester emails, invite/private links and secret markers and is included in `tools/run_final_local_gate.py` plus `tools/verify_release.py`.
- Latest Play pre-launch review evidence packet hardening: `tools/print_pre_launch_review_evidence_packet.py` now prints safe pre-launch result, no reproducible crashes and Play policy warnings evidence lines for `play_store/play_console_post_upload_evidence_ru.md` after the owner actually reviews the Play Console pre-launch report and policy warnings. It rejects secret/email/private-link markers and is included in `tools/run_final_local_gate.py` plus `tools/verify_release.py`.
- Latest store-listing review evidence packet hardening: `tools/print_store_listing_review_evidence_packet.py` now verifies the current local store asset review sheet dimensions and prints the safe `Store listing preview checked for damaging image crops` evidence line after the Play Console preview and review sheet are actually checked. It rejects secret/email/private-link markers and is included in `tools/run_final_local_gate.py` plus `tools/verify_release.py`.
- Latest local signing-ignore extension hardening: `.gitignore` and `tools/verify_release.py` now also cover `*.pem`, `*.pk8` and `*.key`, keeping local ignore hygiene aligned with the remote signing-artifact scan for common Android signing/private-key formats.
- Latest case-insensitive signing-artifact hygiene hardening: `.gitignore` now uses bracket patterns for common signing-key extensions, and `tools/verify_remote_release.py` compiles forbidden remote signing/install path checks with `re.IGNORECASE`; `tools/verify_release.py` proves uppercase `.P12`, `.JKS`, `.KEYSTORE`, `.PEM`, `.PK8`, `.KEY`, `.APK`, `.APKS` and `.IDSIG` paths are rejected.
- Latest local install-artifact ignore case hardening: `.gitignore` now uses bracket patterns for copied Android upload/install artifact extensions (`*.apk`, `*.aab`, `*.apks`, `*.idsig`), so uppercase local copies such as `APP-RELEASE.APK`, `PLAY.AAB`, `BUNDLE.APKS` and `ARTIFACT.IDSIG` stay ignored before they can enter a commit.
- Latest semantic git-ignore verification hardening: `tools/verify_release.py` now invokes `git check-ignore` for representative lower/upper-case `local.properties`, `keystore.properties`, signing-key and Android install/upload artifact paths, proving `.gitignore` behavior rather than only matching text entries.
- Latest local tracked forbidden-path hardening: `tools/verify_release.py` now scans `git ls-files` and fails before push if forbidden sensitive/install paths are tracked locally, while allowing only the intentional signed release AAB at `app/build/outputs/bundle/release/app-release.aab`.
- Latest deterministic AAB hardening: `app/build.gradle.kts` disables `extractReleaseVersionControlInfo` so the tracked signed AAB does not package `base/root/META-INF/version-control-info.textproto` and does not change checksum after every release commit. `tools/verify_release.py` gates both the build config and the absence of that AAB entry.
- Latest positive owner-gate evidence hardening: `tools/print_publication_readiness.py` no longer treats `none` or mixed negative phrases such as `yes, but not completed` as valid positive confirmations for required owner gates such as populated Play Console support/contact fields. `tools/verify_release.py` proves that both bad values are rejected before production-ready output can be confirmed.
- Latest signing preflight evidence validation hardening: `tools/print_publication_readiness.py` now validates signing-backup evidence lines for exact active keystore path, `keystore.properties` target, `qgrid_upload` alias, owner-only `0o600` modes and hidden password wording. `tools/verify_release.py` proves wrong file modes, wrong active storeFile, wrong alias and missing password-present wording are rejected before production-ready output can be confirmed.
- Latest non-empty owner evidence hardening: `tools/print_publication_readiness.py` now rejects weak non-empty values such as `pending`, `unknown`, `not available locally` and `todo` for owner evidence fields that require a real recorded value, including responsible owner, backup storage type and backup record location. `tools/verify_release.py` proves these weak values are rejected before production-ready output can be confirmed.
- Latest owner date evidence hardening: `tools/print_publication_readiness.py` now rejects year-only or negative date strings such as `2026`, `pending 2026-06-06` and `2026 unknown` for upload and signing-backup date/time evidence. `tools/verify_release.py` proves both upload date/time and backup date/time require a concrete full date such as `2026-06-06`.
- Latest testing-first release-track evidence hardening: `tools/print_publication_readiness.py` now rejects any `First release track used` value other than `internal testing`, because `play_store/upload_runbook_ru.md` requires uploading first through internal testing and records required closed testing separately. `tools/verify_release.py` proves both direct production and closed-testing-as-first-track evidence cannot unlock production-ready output.
- Latest internal-testing upload evidence hardening: `tools/print_publication_readiness.py` now rejects bare `yes` for `Internal testing upload completed`; owner evidence must explicitly mention `internal testing` and that the AAB was uploaded or upload completed.
- Latest closed-testing tester-duration evidence hardening: `tools/print_publication_readiness.py` now rejects completed closed-testing evidence that omits the required at least 12 opted-in testers and at least 14 continuous days, and rejects tester emails, URLs or private tester links in safe evidence.
- Latest closed-testing handoff hardening: `play_store/closed_testing_handoff_ru.md` now gives the owner a safe closed-test setup checklist, private tester task script, aggregate feedback topics and exact publication-readiness evidence phrases without storing tester personal data. `tools/print_play_console_packet.py`, `tools/prepare_play_upload_archive.py` and `tools/verify_release.py` gate the handoff and include it in the owner ZIP.
- Latest Play pre-launch/policy evidence hardening: `tools/print_publication_readiness.py` now rejects bare `passed`/`none` for pre-launch and policy review fields; owner evidence must explicitly mention the Play Console pre-launch report result, no reproducible crashes and Play policy warnings being absent/unresolved or resolved.
- Latest closed-testing evidence consistency hardening: `tools/print_publication_readiness.py` now validates `Closed testing required for this account` together with `Closed testing status if required`, so `yes` must pair with `completed required closed testing` and `no` must pair with `not required for this account`. `tools/verify_release.py` proves both contradictory pairs are rejected before production-ready output can be confirmed.
- Latest production-access owner evidence hardening: `tools/print_publication_readiness.py` now requires `Production access status if required` after the closed-testing path. If closed testing is required, owner evidence must explicitly say Play Console production access was granted or approved; if not required, it must say `not required for this account`. `tools/verify_release.py` proves pending/requested/contradictory production-access evidence cannot unlock production-ready output.
- Latest production-access handoff alignment hardening: `README.md`, `AGENTS.md`, `docs/requirements_traceability.md`, `docs/google_play_checklist.md`, `docs/release_plan.md`, `play_store/upload_runbook_ru.md`, `play_store/play_console_submission_ru.md`, `play_store/app_content_answers_ru.md`, `play_store/owner_release_inputs.md`, `play_store/upload_manifest.md` and `tools/print_play_console_packet.py` now all preserve the same production-access owner gate. `tools/verify_release.py` gates those markers so upload-day instructions cannot drift back to only saying closed testing.
- Latest production-access answer worksheet hardening: `play_store/production_access_answers_ru.md` now gives the owner safe copy-ready structure for Play Console `Apply for production access` questions covering closed-test summary, app/game value and production readiness without tester personal data. `tools/print_play_console_packet.py`, `tools/prepare_play_upload_archive.py` and `tools/verify_release.py` gate the worksheet and include it in the owner handoff ZIP.
- Latest owner evidence secret-pattern hardening: `tools/print_publication_readiness.py` now rejects owner evidence that records signing passwords, client secrets, access tokens, refresh tokens or bearer credentials in alternate field-assignment formats. `tools/verify_release.py` proves these secret-pattern cases are rejected while the safe local signing preflight wording remains accepted.
- Latest publication-readiness owner-action breakdown: `tools/print_publication_readiness.py` now groups unresolved external publication gates by owner action, evidence file and required command, covering upload artifact identity, privacy/contact, signing backup, Play-generated artifact review, Play Console forms and testing/final review.
- Latest owner-action handoff archive inclusion: `play_store/publication_readiness_owner_actions_ru.md` now preserves the unresolved owner-action breakdown in the release-facing Play handoff, and the generated owner ZIP includes it under `_owner_handoff`.
- Latest owner-action handoff synchronization gate: `tools/verify_release.py` now imports `tools/print_publication_readiness.py` and verifies that every owner-action handoff section matches `OWNER_ACTION_GROUPS` title, action, evidence files, commands and evidence field labels.
- Latest final local gate after owner-action sync: `./tools/run_final_local_gate.py` passed with `final_local_gate_ok` after the synchronized owner-action handoff and regenerated owner ZIP.
- Latest completed owner-evidence fixture gate: `tools/verify_release.py` now proves fully completed safe post-upload and signing-backup evidence fixtures are accepted by `tools/print_publication_readiness.py` with zero unresolved fields.
- Latest managed API 36 continuation gate: `./tools/run_api36_connected_gate.py` passed end-to-end on 6 June 2026. The managed clean `Medium_Phone_API_36` connected suite finished 10/10 tests with 0 skipped and 0 failed tests, and the follow-up owner preflight returned `release_verification_ok`, `play_upload_archive_existing_ok`, `publication_readiness_local_ready_external_pending`, `final_local_gate_ok` and `api36_connected_gate_ok`.
- Latest release relaunch helper date hardening: `tools/capture_release_relaunch_smoke.py` now records the actual run date with `date.today().isoformat()`, and `tools/verify_release.py` validates `checked_on=YYYY-MM-DD` instead of requiring one fixed date string.
- Latest release relaunch API guard hardening: `tools/capture_release_relaunch_smoke.py` now requires API 36 by default before installing the release APK or writing relaunch evidence, and `tools/verify_release.py` proves an API 35 target is rejected unless the owner explicitly opts into diagnostic `--expected-api any`.
- Latest release relaunch input-normalization hardening: `tools/capture_release_relaunch_smoke.py` now rejects malformed `--expected-api` values, normalizes accepted API values before evidence output and strips emulator-console `OK` noise from future AVD-name evidence; `tools/verify_release.py` gates these helper behaviors.
- Latest release relaunch strict API-label hardening: `tools/capture_release_relaunch_smoke.py` now rejects leading-zero API labels such as `036` and `00`, and `tools/verify_release.py` proves the AVD-name cleaner strips emulator-console `OK` noise.
- Latest release relaunch diagnostic-evidence gate: `tools/verify_release.py` now rejects future relaunch evidence that records `expected_api=any`, so diagnostic smoke runs cannot accidentally count as release evidence. Existing pre-guard API 36 evidence without an `expected_api` line remains accepted.
- Latest fresh release relaunch evidence refresh: `./tools/capture_release_relaunch_smoke.py --serial emulator-5554` passed on `Medium_Phone_API_36` / API 36 with refreshed evidence that records `expected_api=36`, a clean one-line AVD name, HOME/background return, force-stop/relaunch and no matching app crash-buffer entries. `tools/verify_release.py` now requires `expected_api=36` in release relaunch evidence.
- Latest release relaunch freshness gate: `tools/verify_release.py` now requires the relaunch state and screenshot evidence to be newer than both `tools/capture_release_relaunch_smoke.py` and the generated release APK, preventing stale release-relaunch QA evidence after helper or APK changes.
- Latest managed API36 connected gate after dirty-emulator cleanup: `./tools/run_api36_connected_gate.py` passed after the final local runner gained stale package/process cleanup. The clean `Medium_Phone_API_36` run on `emulator-5560` completed `connectedDebugAndroidTest` 10/10 with 0 skipped and 0 failed tests, then returned `release_verification_ok`, `play_upload_archive_existing_ok`, `publication_readiness_local_ready_external_pending`, `final_local_gate_ok` and `api36_connected_gate_ok`.
- Latest serial-scoped API36 connected final-gate refresh: `./tools/run_final_local_gate.py --include-connected --connected-serial emulator-5554` passed on `Medium_Phone_API_36` / API 36. The optional connected path refreshed the mandatory XML evidence to 10 tests, 0 failures, 0 errors and 0 skipped, and the full local owner preflight returned `release_verification_ok`, `upload_packet_ok`, `play_console_packet_ok`, `privacy_policy_local_ok`, `signing_backup_input_ok` and `final_local_gate_ok`. Production readiness still remains externally gated by Play Console upload evidence, Play Console privacy URL entry/contact fields, owner signing backup evidence and testing-track/pre-launch evidence.
- Latest upload-day preflight helper hardening: `tools/run_upload_day_preflight.py` now wraps the owner upload-day sequence without hiding external gates. It runs hosted privacy validation through the final local gate or managed API 36 connected gate, regenerates the internal store asset review sheet, writes and verifies the owner upload ZIP, reprints upload and Play Console packets, reruns publication readiness with recorded hosted privacy URL validation and can include remote annotated-tag verification after push. `tools/verify_release.py` gates the helper dry-run modes.
- Latest managed upload-day retry and foreground-recovery hardening: a managed upload-day preflight exposed API 36 infrastructure instability where UTP reported `device offline` with zero tests and the managed helper returned signal `-15`. `tools/run_upload_day_preflight.py` now reruns the managed API 36 gate once for that terminated-helper path, while ordinary connected test failures still fail. `Line56AppSmokeTest` also brings the target app launcher activity back to the foreground before each test and inside `ensureHome()` so system Settings foreground state does not break release evidence.
- Latest full upload-day preflight for `v1.0.0-rc60`: `./tools/run_upload_day_preflight.py --managed-api36-connected --release-tag v1.0.0-rc60` passed on 12 June 2026. Evidence includes managed API 36 connected tests 10/10 with 0 skipped and 0 failed tests, `release_verification_ok`, `final_local_gate_ok`, `api36_connected_gate_ok`, regenerated `play_store/store_asset_review_sheet.png`, verified owner handoff ZIP `build/play_upload/line56_v1_google_play_upload_packet.zip`, `publication_readiness_local_ready_external_pending`, hosted privacy URL validation, `remote_release_ok` and final `upload_day_preflight_ok`.
- Latest Play developer-account gate hardening on 12 June 2026: official Google Play policy sources were spot-checked again, including the 28 May 2026 Developer Program Policy update for Android developer verification and package-name requirements. Publication readiness now has explicit external evidence fields for Play Console developer identity verification, developer profile/contact completion and `com.qgrid.mobile` package-name registration, with safe output provided by `tools/print_developer_account_evidence_packet.py` and verifier coverage in `tools/verify_release.py`.
- Latest current-tag upload-day preflight for `v1.0.0-rc63`: `./tools/run_upload_day_preflight.py --release-tag v1.0.0-rc63` passed on 12 June 2026 after the developer-account gate hardening. Evidence includes hosted final local gate validation, regenerated internal store asset review sheet, verified owner handoff ZIP, `publication_readiness_local_ready_external_pending`, hosted privacy URL validation, annotated remote tag verification, 13 remote upload assets verified from `play_store/upload_checksums.md`, remote forbidden-path/AAB scans and final `remote_release_ok` plus `upload_day_preflight_ok`.
- Latest upload-runbook sequence sync hardening: `play_store/upload_runbook_ru.md` now lists the exact default `./tools/run_final_local_gate.py --dry-run` command order in its equivalent expanded sequence, including `./tools/print_play_generated_apk_evidence_packet.py --dry-run`. `tools/verify_release.py` now parses that fenced block and fails if the order or command set drifts from the final local gate.
- Remote upload-day preflight evidence for `v1.0.0-rc64`: `./tools/run_upload_day_preflight.py --release-tag v1.0.0-rc64` passed on 12 June 2026 after the upload-runbook sequence sync hardening. Evidence includes hosted final local gate validation, regenerated internal store asset review sheet, verified owner handoff ZIP, `publication_readiness_local_ready_external_pending`, hosted privacy URL validation, annotated remote tag verification, 13 remote upload assets verified from `play_store/upload_checksums.md`, remote forbidden-path/AAB scans and final `remote_release_ok` plus `upload_day_preflight_ok`.
- Latest Google Play checklist final-gate sequence sync hardening: `docs/google_play_checklist.md` now uses the same exact equivalent expanded sequence as `./tools/run_final_local_gate.py --dry-run`, and `tools/verify_release.py` reuses a single `FINAL_LOCAL_GATE_SEQUENCE` assertion for both the checklist and `play_store/upload_runbook_ru.md`.

## Not Yet Fully Proven Final

- Play Console developer identity/profile confirmation and `com.qgrid.mobile` package-name registration require account access and cannot be completed locally.
- Privacy policy text and HTML are hosted at `https://xarok3267742-ai.github.io/56game/privacy_policy_ru.html` and passed `privacy_policy_url_ok`, but final publication still needs that URL entered in Play Console plus populated Play Console support/contact fields.
- Play Console forms are not completed because they require account access.
- Closed testing and any required Play Console production-access approval cannot be completed locally; account type, testers and Play review are external.
- Owner release inputs in `play_store/owner_release_inputs.md` require real owner decisions before upload.
- Signing backup local inputs are preflight-checked and safely recorded, but the actual secure backup remains owner-controlled and must be recorded without secrets.

## Current Verdict

The project is a strong signed code release candidate with real screenshots, a Play-format icon, a Play-format feature graphic, verified Android builds and a verifier-gated owner-input handoff. It is not yet a fully publication-complete Google Play product because Play Console developer identity/profile confirmation, `com.qgrid.mobile` package-name registration, Play Console privacy URL entry, Play Console support/contact fields, signing backup, testing-track evidence, production-access approval if required and other Play Console/account actions remain outside the local codebase.
